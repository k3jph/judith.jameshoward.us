import type { Metadata } from "next";
import { PageShell, SectionIntro } from "../components/site-chrome";
import { artworks, bibliography } from "../data/exhibition";
export const metadata:Metadata={title:"Bibliography & Sources",description:"Institutional object records and scholarship supporting The Fatal Image."};
export default function SourcesPage(){return <PageShell className="reference-page"><SectionIntro eyebrow="Recoverable evidence" title="Bibliography & Sources"><p>The label should never be the end of inquiry. Every catalogue entry links to its best available public object record or to an explicitly labelled surrogate when no holding-institution record is public; selected interpretive claims carry additional source links on the object page where they are used.</p></SectionIntro><section className="source-section"><p className="eyebrow">Object and discovery records</p><h2>Records, with their limits visible</h2><ol className="object-sources">{artworks.map(w=><li key={w.slug}><span>{w.artist}, <i>{w.title}</i>, {w.dateDisplay}</span><a href={w.primarySource} target="_blank" rel="noreferrer">{w.sourceKind || w.collection} ↗</a></li>)}</ol></section><section className="source-section" id="artemisia-scholarship"><p className="eyebrow">Selected scholarship</p><h2>Exhibition bibliography</h2><ol className="bibliography">{bibliography.map((entry,i)=><li key={entry.citation}><span>{String(i+1).padStart(2,"0")}</span>{entry.url?<a href={entry.url} target="_blank" rel="noreferrer">{entry.citation} ↗</a>:entry.citation}</li>)}</ol></section><section className="source-section source-policy"><p className="eyebrow">Citation policy</p><h2>What a citation means here</h2><div><p>Object data follows a holding institution when one publishes a record. A specialist database, artist statement, dealer survey, or commercial listing is labelled as such rather than promoted into a museum record. Dates, places, dimensions, and attributions expose known conflicts instead of silently choosing the tidiest answer.</p><p>Curatorial synthesis is the exhibition's own unless a linked source is named. The bibliography supplies the broader conversation; object-page source lists let a reader trace claims about disputed titles, biography, civic reception, and textual history to the evidence actually used.</p></div></section></PageShell>}
